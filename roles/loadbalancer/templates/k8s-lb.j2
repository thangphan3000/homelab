stream {
  upstream kubernetes {
  {% for host in lb.k8s_masters %}
      server {{ host.ip }}:{{ lb.kube_proxy_port }};
  {% endfor %}
  }

  server {
      listen {{ lb.kube_proxy_port }};
      proxy_pass kubernetes;
  }
}
